#include <stdio.h>

int main() {
    int x;
    long long y;
    scanf("%d %lld", &x, &y);
    printf("%d + %lld = %lld\n", x, y, (long long)x + y);
    printf("%d * %lld = %lld\n", x, y, (long long)x * y);
    printf("%d - %lld = %lld\n", x, y, (long long)x - y);
    return 0;
}
