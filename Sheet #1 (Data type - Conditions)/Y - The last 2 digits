#include <stdio.h>

int main()
{
    long long a, b, c, d;
    int m;
    scanf("%lld %lld %lld %lld", &a, &b, &c, &d);
    a = a % 100;
    b = b % 100;
    c = c % 100;
    d = d % 100;
    m = (a * b * c * d) % 100;
    printf("%02d\n", m);
}
